import gamefile
def name():
        return "Genesis"
def colour():
	return (215,162,0)
def commands():
        gamefile.move(500)
	gamefile.fire()
	gamefile.turn_right(90)
	gamefile.fire()	
	gamefile.stop(10)
	gamefile.fire()	
	gamefile.move(500)
	gamefile.fire()
	gamefile.turn_right(90)
	gamefile.fire()
	gamefile.done()

def target_spotted(direction):
        gamefile.pointgun((direction)*1.3)
        gamefile.fire()
	
